# scylla-ansible
Ansible palybook for deploy scyllaDB on production environment

## Modules
Make sure you are installed all needed modules with below command:

```bash
ansible-galaxy collection install -r requirements.yaml
```


